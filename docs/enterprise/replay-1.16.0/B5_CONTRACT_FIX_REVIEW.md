# Dify Enterprise 1.16.0 Replay B5 Contract Fix — Independent Review

## RECOVERY

- expected/actual branch: `ctyun/replay-116-b5-contract-reviewer` / `ctyun/replay-116-b5-contract-reviewer` — PASS
- expected/actual HEAD: `7237af7c759f433fac9e2e2c1a1b63d816134a24` / `7237af7c759f433fac9e2e2c1a1b63d816134a24` — PASS
- expected/actual parent: `55cfe8ec165f3799543fabbe2d777d332cf1de0e` / `55cfe8ec165f3799543fabbe2d777d332cf1de0e` — PASS
- clean at original review start: yes; `git status --porcelain=v1` was empty.
- environment-validation start: porcelain contained only the pre-existing intent-to-add report, ` A docs/enterprise/replay-1.16.0/B5_CONTRACT_FIX_REVIEW.md`; no other tracked path was dirty.
- B4 checkpoint ancestor: yes; `git merge-base --is-ancestor 9c4c0356f3f2374c22b383ba96331e1dd92505fd HEAD` exit 0.
- `verify_git_start.sh` result: exit 0; `OK branch=ctyun/replay-116-b5-contract-reviewer head=7237af7c759f433fac9e2e2c1a1b63d816134a24 clean=true`.
- No reset, merge, rebase, checkout, cherry-pick, source fix, generated-file fix, commit, amend, push, or B5 startup was performed.

## SOURCES_READ

Required documents, in order:

1. `docs/enterprise/replay-1.16.0/CURRENT_STATE.md`, especially §§4, 7, 8; its pre-Fixer role/origin snapshot was treated as historical as instructed.
2. `docs/enterprise/replay-1.16.0/B5_IMPLEMENTATION_PLAN.md`, especially §§3.2, 12, 13.2, 13.3, 17.
3. `docs/enterprise/replay-1.16.0/B5_IMPLEMENTATION_PLAN_REREVIEW.md`.
4. `docs/enterprise/replay-1.16.0/B4_FINAL_REREVIEW.md`.
5. `api/AGENTS.md`.
6. `api/controllers/API_SCHEMA_GUIDE.md`.
7. Exact-range stat, name-status, numstat, full diff, and diff-check.

Implementation, exception, handler, test, and generated-contract evidence read:

- `api/controllers/console/platform_admin.py`
- `api/controllers/console/enterprise_marketplace.py`
- `api/libs/platform_admin.py`
- `api/libs/exception.py`
- `api/libs/login.py`
- `api/extensions/ext_login.py`
- `api/services/platform_admin_service.py`
- `api/services/enterprise_marketplace_service.py`
- `api/services/errors/enterprise_marketplace.py`
- `api/tests/unit_tests/controllers/console/test_platform_admin.py`
- `api/tests/unit_tests/controllers/console/test_enterprise_marketplace.py`
- `api/tests/unit_tests/services/test_enterprise_marketplace_service.py`
- `packages/contracts/openapi/console-openapi.json`, regenerated by the official command and inspected with `jq`
- `packages/contracts/generated/api/console/account/{types.gen.ts,zod.gen.ts,orpc.gen.ts}`
- `packages/contracts/generated/api/console/platform-admin/{types.gen.ts,zod.gen.ts,orpc.gen.ts}`
- `packages/contracts/generated/api/console/router.gen.ts`
- `packages/contracts/package.json`
- `packages/contracts/openapi-ts.api.config.ts`
- root `package.json`, `pnpm-workspace.yaml`, `pnpm-lock.yaml`, and `api/uv.lock` for the frozen validation environments.

## REVIEW_SCOPE

- reviewed range: `55cfe8ec165f3799543fabbe2d777d332cf1de0e..7237af7c759f433fac9e2e2c1a1b63d816134a24`
- exact changed files:
  1. `api/controllers/console/enterprise_marketplace.py`
  2. `api/controllers/console/platform_admin.py`
  3. `api/tests/unit_tests/controllers/console/test_enterprise_marketplace.py`
  4. `api/tests/unit_tests/controllers/console/test_platform_admin.py`
  5. `packages/contracts/generated/api/console/account/types.gen.ts`
  6. `packages/contracts/generated/api/console/account/zod.gen.ts`
  7. `packages/contracts/generated/api/console/platform-admin/types.gen.ts`
  8. `packages/contracts/generated/api/console/platform-admin/zod.gen.ts`
- expected/actual stat: `8 files changed, 255 insertions(+), 0 deletions(-)` / `8 files changed, 255 insertions(+), 0 deletions(-)`.
- per-file numstat in that order: `1/0`, `51/0`, `14/0`, `86/0`, `12/0`, `10/0`, `72/0`, `9/0`.
- no Web, Docker, migration, model/schema, service state-machine, dependency, manifest, or lockfile change is in the reviewed range.
- no frontend direct fetch, handwritten response/error type, legacy contract loader, or workaround is in the range.
- scope verdict: PASS; the exact eight-file allowlist and expected statistics match.

## B3_CONTRACT_EVIDENCE

### 7/7 route matrix

| Route | Success | Declared/OpenAPI/generated errors | Real reachability evidence | Verdict |
| --- | ---: | --- | --- | --- |
| `GET /console/api/account/platform-admin-status` | 200 | 401 | `login_required`; official unauthorized handler | PASS |
| `GET /console/api/platform-admin/workspaces` | 200 | 400, 401, 403, 409 | query validation; login; platform-admin guard; current-tenant guard | PASS |
| `GET /console/api/platform-admin/workspaces/<workspace_id>` | 200 | 401, 403, 404, 409 | login/admin; missing workspace; current-tenant guard | PASS |
| `PATCH /console/api/platform-admin/workspaces/<workspace_id>` | 200 | 400, 401, 403, 404, 409 | payload validation; login/admin; missing workspace; unavailable/concurrent/current-tenant conflict | PASS |
| `GET /console/api/platform-admin/workspaces/<workspace_id>/members` | 200 | 401, 403, 404, 409 | login/admin; missing workspace; current-tenant guard | PASS |
| `POST /console/api/platform-admin/workspaces/<workspace_id>/members/invitations` | 201 | 400, 401, 403, 404, 409, 503 | request/service validation; auth; lookups/conflicts; RBAC mutation fail-closed | PASS |
| `PATCH /console/api/platform-admin/workspaces/<workspace_id>/members/<member_id>/role` | 200 | 400, 401, 403, 404, 409, 503 | request/service validation; auth; workspace/member lookup; conflicts; RBAC mutation fail-closed | PASS |

Reachability and schema separation:

- `api/extensions/ext_login.py:168-177` returns official 401 JSON `{code,message}`. Every reviewed 401 response references `UnauthorizedResponse`; no 401 uses a domain DTO.
- `PlatformAdminHTTPError` inherits `BaseHTTPException`; `api/libs/exception.py:6-17` supplies domain JSON `{code,message,status}`.
- `api/libs/platform_admin.py:71-78` makes 403 reachable and `:83-95` makes 409 reachable on every management route.
- Pydantic request/query validation makes the declared 400 statuses reachable.
- Workspace/member lookup and persistence paths in `api/services/platform_admin_service.py:163-174`, `:200-214`, and `:337-360` make the declared 404 statuses reachable.
- RBAC-enabled invitation and role mutations in `api/services/platform_admin_service.py:223-240` and `:319-335` fail closed with 503.
- The regenerated OpenAPI schemas require exactly `code,message` for `UnauthorizedResponse` and exactly `code,message,status` for `PlatformAdminErrorResponse`.
- The regenerated OpenAPI response keys and `$ref`s match all seven rows, including invitation success 201 and domain/401 separation.
- `GetAccountPlatformAdminStatusErrors` is exactly `{401: UnauthorizedResponse}`. The six platform-admin workspace/member TypeScript `*Errors` maps exactly match the matrix.
- Zod uses a two-string object for unauthorized and `{code:z.string(),message:z.string(),status:z.int()}` for platform-admin domain errors, consistent with TypeScript.
- Adjacent account/platform-admin `orpc.gen.ts` and top-level `router.gen.ts` preserve the expected operation IDs, methods, paths, and nesting.
- The focused OpenAPI assertions passed after official generation as part of the `97 passed` suite.

## B4_422_EVIDENCE

### Reachable 422 paths

- `MarketplaceReviewApi.post` calls `EnterpriseMarketplaceService.approve_asset` for `approved` decisions (`api/controllers/console/enterprise_marketplace.py:581-601`).
- Approval calls `_validate_dsl_no_secrets` and `_extract_and_normalize_dependencies` (`api/services/enterprise_marketplace_service.py:233-234`) and performs the icon-link portability check (`:257-258`).
- The sanitizer path (`:789-848`) can raise `SnapshotContainsSecret` and `NonportableResourceReference`; dependency normalization (`:853-866`) can raise `PrivatePluginDependency`.
- Those domain exceptions have status 422 in `api/services/errors/enterprise_marketplace.py:81-97`.
- The controller catches `MarketplaceError` and maps it through `MarketplaceHTTPError` without changing business behavior (`api/controllers/console/enterprise_marketplace.py:367-368`, `:600-601`).

Controller/OpenAPI/generated evidence:

- The route declares `@_err_response(422, _ERR_422)` at `api/controllers/console/enterprise_marketplace.py:574`.
- The regenerated OpenAPI review operation contains exactly errors `400,401,403,404,409,422`; 422 references `MarketplaceErrorResponse`.
- `MarketplaceErrorResponse` requires `{code,message,status}`; review 401 references the two-field `UnauthorizedResponse`; all review domain statuses reference `MarketplaceErrorResponse`.
- `PostPlatformAdminEnterpriseMarketplaceAssetsByAssetIdReviewsErrors` contains exactly `400,401,403,404,409,422`, with the same 401/domain split.
- TypeScript and Zod Marketplace error schemas agree.
- The binding corrected acceptance matrix explicitly excludes unreachable review 503. No review 503 finding or acceptance condition remains.
- No service, domain exception, state-machine, permission, or business-behavior file changed in the commit; the B4 change is declaration/test/generated-contract only.

## GENERATION

The official script is the `gen-api-contract` script in `packages/contracts/package.json`. Because the sandbox's shared UV cache is read-only, successful runs used only the explicitly authorized isolated UV cache in addition to the required cleared proxies and Node 22 PATH.

### First successful generation

- before: `git status --porcelain=v1` and `git diff --name-status` showed only the pre-existing report; generated-console diff empty; `git diff --check` exit 0.
- literal required command with cleared proxies and Node 22 PATH but no isolated UV cache: exit 2 at UV cache locking; no tracked diff.
- successful command: `env ALL_PROXY= all_proxy= HTTP_PROXY= HTTPS_PROXY= http_proxy= https_proxy= UV_CACHE_DIR=/tmp/replay-116-b5-contract-reviewer-uv-cache PATH=/home/ctyun/BigData/.nvm/versions/node/v22.22.2/bin:$PATH pnpm --dir packages/contracts gen-api-contract`
- result: exit 0; Swagger/FastOpenAPI generation, 60 OpenAPI TypeScript jobs, and `vp fmt generated/api` completed.
- after: status/name-status still only the report; `git diff -- packages/contracts/generated/api/console` empty; lockfile diff empty; `git diff --check` exit 0.
- first-generation additional tracked diff: none.

### Second successful generation

- before: status/name-status only the report; generated-console diff empty; `git diff --check` exit 0.
- exact successful command: same isolated-cache, proxy-cleared, Node 22 command above.
- result: exit 0; generator and formatter completed again.
- after: status/name-status only the report; generated-console diff empty; lockfile diff empty; `git diff --check` exit 0.
- second-generation additional tracked diff: none.
- deterministic verdict: PASS; two consecutive successful official generations reproduced the tracked artifacts with zero additional tracked difference.

Lock determinism:

- before and after `api/uv.lock`: `2903bfd7fb58511054a0d7111717eb8be92eac627f80862517f09fe69eeffed2`.
- before and after `pnpm-lock.yaml`: `62f3e0f3639dd80d1d058cae38a3dca53fc5b626fb2e6bec446a0aa397148ee7`.
- both lockfile diffs remained empty.

## FINDINGS

- P0: 0
- P1: 0
- P2: 1

### B5CR-02 — P2 — Contracted four-file targeted Ruff gate fails

- locations:
  - `api/tests/unit_tests/controllers/console/test_enterprise_marketplace.py:46` — PT018.
  - `api/tests/unit_tests/controllers/console/test_enterprise_marketplace.py:144` — PLR1714 and E501.
  - `api/tests/unit_tests/controllers/console/test_enterprise_marketplace.py:578` — E501.
  - `api/tests/unit_tests/controllers/console/test_enterprise_marketplace.py:753`, `:771`, `:793`, and `:823` — FURB171.
  - file-level formatter findings: `api/controllers/console/enterprise_marketplace.py:1`, `api/tests/unit_tests/controllers/console/test_enterprise_marketplace.py:1`, and `api/tests/unit_tests/controllers/console/test_platform_admin.py:1` (Ruff reports each complete file “Would reformat”).
- reproduction commands:
  - `UV_CACHE_DIR=/tmp/replay-116-b5-contract-reviewer-uv-cache uv run --project api --no-sync ruff check api/controllers/console/enterprise_marketplace.py api/controllers/console/platform_admin.py api/tests/unit_tests/controllers/console/test_enterprise_marketplace.py api/tests/unit_tests/controllers/console/test_platform_admin.py`
  - `UV_CACHE_DIR=/tmp/replay-116-b5-contract-reviewer-uv-cache uv run --project api --no-sync ruff format --check api/controllers/console/enterprise_marketplace.py api/controllers/console/platform_admin.py api/tests/unit_tests/controllers/console/test_enterprise_marketplace.py api/tests/unit_tests/controllers/console/test_platform_admin.py`
- actual result: lint command exit 1 with 8 violations; format-check exit 1 with 3 files requiring reformat and 1 already formatted.
- violated contract/invariant: the Reviewer contract requires all four changed Python files to pass both exact targeted read-only Ruff gates and states that a targeted Ruff failure must be a finding, irrespective of the accepted full-API historical baseline.
- minimal repair boundary: only the reported lint expressions and formatting in the three reported files, followed by the same focused tests, full regression, targeted Ruff, and two-generation checks. Do not expand to full-API Ruff, frontend, service behavior, dependencies, or lockfiles.
- blocks B5: yes.

`B5CR-01` remains CLOSED as the coordinator's binding task-contract correction: marketplace review errors are exactly `400,401,403,404,409,422`; review 503 is neither reachable nor required. It is not an open product finding.

Open findings: P0/P1/P2 = `0/0/1`.

## COMMANDS

| Command | Exit | Accurate result |
| --- | ---: | --- |
| startup Git commands and B4 ancestor check | 0 each | expected branch, HEAD, parent, clean start, ancestor=true |
| `verify_git_start.sh "$(pwd)" ...` | 0 | exact branch/SHA and clean=true |
| exact-range stat/name-status/numstat/diff/check | 0 each | exact 8 files, 255 insertions, no deletions, diff-check clean |
| baseline `sha256sum api/uv.lock pnpm-lock.yaml` | 0 | hashes recorded above |
| sandbox `UV_CACHE_DIR=... uv sync --project api --frozen --group dev` | 1 | pinned Git dependency fetch blocked by restricted network; partial ignored venv only |
| approved-network rerun of the same frozen UV sync | 0 | CPython 3.12.13; 513 locked packages installed; no lockfile change |
| Node 22 `node --version` | 0 | `v22.22.2` |
| isolated `pnpm --version` first sandbox attempt | 1 | Corepack network/proxy blocked |
| isolated `pnpm --version` approved-network rerun | 0 | `11.10.0` |
| frozen filtered pnpm install first sandbox attempt | 130 | lockfile already up to date; interrupted during repeated network EPERM retries |
| approved-network rerun of frozen filtered pnpm install | 0 | scope 2/12; lockfile resolution skipped; 507 packages materialized; no lockfile change |
| literal focused pytest | 2 | UV shared cache read-only; no collection |
| isolated-cache focused pytest with inherited proxy | 4 | no collection; `httpx` rejected injected `socks://` proxy URL |
| isolated-cache/proxy-cleared focused pytest before OpenAPI generation | 1 | 97 collected: 86 passed, 11 setup errors from absent ignored OpenAPI JSON, 0 failed, 0 skipped, 2 warnings |
| first successful official generation | 0 | completed; zero additional tracked diff |
| isolated-cache/proxy-cleared focused pytest after generation | 0 | 97 collected, 97 passed, 0 failed, 0 skipped, 2 warnings |
| isolated-cache/proxy-cleared nine-file regression | 0 | 403 collected, 403 passed, 0 failed, 0 skipped, 165 warnings |
| `PATH=...node/v22.22.2/bin:$PATH pnpm --dir packages/contracts test` | 0 | 1 test file passed; 4 tests passed; 0 failed/skipped |
| `PATH=...node/v22.22.2/bin:$PATH pnpm --dir packages/contracts type-check` | 0 | `tsc` completed successfully |
| literal targeted `ruff check` | 2 | UV shared cache read-only; Ruff did not run |
| isolated-cache targeted `ruff check` | 1 | 8 violations; FAILED |
| literal targeted `ruff format --check` | 2 | UV shared cache read-only; Ruff did not run |
| isolated-cache targeted `ruff format --check` | 1 | 3 files would be reformatted, 1 already formatted; FAILED |
| second successful official generation | 0 | completed; zero additional tracked diff |
| OpenAPI `jq` route/schema inspection | 0 | all B3 rows and B4 review matrix/schema refs match evidence above |
| final `sha256sum api/uv.lock pnpm-lock.yaml` | 0 | exact baseline hashes retained |
| removal of only `api/.venv` and the two named `/tmp` roots | 0 | authorized temporary environments removed |
| post-cleanup absence checks | 0 | all three named paths absent |
| final tracked/generated/lock diffs and `git diff --check` | 0 | only report dirty; generated and lockfile diffs empty; diff-check clean |

The LiteLLM remote model-cost lookup emitted a fallback warning while proxies/network were cleared, but test execution continued using its local backup. Pytest's reported warning totals are stated above and are not hidden.

## NOT_RUN

- frontend unit tests, browser, and E2E: NOT_RUN — outside this Contract Reviewer scope.
- database writes and migration upgrade/downgrade/stamp/runtime rehearsal: NOT_RUN — explicitly forbidden.
- Docker/container/image/volume operations: NOT_RUN — explicitly forbidden.
- vector-store and external-service writes: NOT_RUN — explicitly forbidden.
- offline build, upgrade, rollback, and B5 runtime validation: NOT_RUN — outside this phase and explicitly not authorized.
- full-API Ruff and any Ruff auto-fix: NOT_RUN — explicitly forbidden; only the exact four-file read-only gates ran.
- no mandatory Contract Reviewer execution gate is NOT_RUN; the mandatory gates executed, with the targeted Ruff gates failing as recorded.

## GIT

- range `git diff --check`: exit 0.
- final working-tree `git diff --check`: exit 0.
- final `git status --short --branch`: branch `ctyun/replay-116-b5-contract-reviewer`; only ` A docs/enterprise/replay-1.16.0/B5_CONTRACT_FIX_REVIEW.md`.
- final `git status --porcelain=v1`: ` A docs/enterprise/replay-1.16.0/B5_CONTRACT_FIX_REVIEW.md`.
- exact modified files: only `docs/enterprise/replay-1.16.0/B5_CONTRACT_FIX_REVIEW.md`; its index entry remains an intent-to-add empty-blob placeholder, cached diff empty.
- generated-console tracked diff: empty.
- `api/uv.lock` and `pnpm-lock.yaml` tracked diff: empty; hashes unchanged.
- authorized temporary `api/.venv`, `/tmp/replay-116-b5-contract-reviewer-uv-cache`, and `/tmp/replay-116-b5-contract-reviewer-pnpm`: absent after cleanup.
- commit: NOT_COMMITTED.
- amend: NOT_AMENDED.
- push: NO_PUSH.

## GATE

- **CHANGES_REQUIRED**
- `CONTRACT_FIX_ACCEPTED=no`
- `BLOCKED_PENDING_CONTRACT_FIX=retained`
- `B5_BUILDER_NOT_AUTHORIZED`
- `HUMAN_DECISION_REQUIRED`

Rationale: the B3 typed error unions and B4 reachable 422 contract are functionally complete, the eight-file scope is exact, all Python and contract tests pass, type-check passes, lockfiles are unchanged, and two official generation passes are deterministic. PASS is nevertheless forbidden because the contracted targeted Ruff check and format-check fail. Open findings are P0/P1/P2 = `0/0/1`; Reviewer made no fix and stops pending coordinator inspection and a finding-scoped decision.
