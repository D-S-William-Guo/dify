# Dify Enterprise Replay 协调窗口 Handoff

时间：2026-08-17
来源：本窗口（Codex + DeepSeek V4 flash）
用途：模型切换/上下文污染/降级时交给新 Codex 窗口。

## 1. 仓库状态

```bash
cd /home/ctyun/BigData/GitHub/dify-enterprise-1.16.0
git status --short --branch
git rev-parse HEAD
git rev-parse origin/codex/enterprise-candidate-1.16.0-20260718
```

预期：HEAD == origin == `a12434195c1e2521d154d9b227bde8a484c320fd`，clean。
注意：`docs/enterprise/replay-1.16.0/PRODUCTION_PLAN_B_RUNBOOK.md` 是本窗口未提交草稿，是否保留由用户决定。

## 2. 已完成

- B0–B8 全链 + Phase D/F/G/H 闭环，最终验证总结已 push。
- 最终 HEAD（重放）：`a12434195c`（含生产/灰度环境存档与决策）。
- 真实 secret 扫描执行过：FAIL，原因=生产使用开发默认 secret，未轮换前不可 PASS。
- 生产 62-6 / 灰度 62-7 环境信息已存档：`PRODUCTION_ENVIRONMENTS.md`。

## 3. 已确认决策

1. 基线 1.16.0；1.16.1 等本轮方法验证后再做。
2. 现有 `docker-1161-*`（Plan A）保留不动。
3. 本轮按标准方式部署为 Plan B，保留同事习惯，不碰 `dify-api-expand`。
4. 生产/灰度各用一套不同随机 secret，轮换一次长期复用。

## 4. 下一步

1. Secret 轮换 runbook（生成两套随机值，不提交）。
2. 重建 1.16.0 离线包并真实 pattern 复扫。
3. Plan B 部署灰度 → 验证 → 生产。
4. 之后用同一套方法验证 1.16.1。

## 5. 禁止

- 不碰 `dify-api-expand`。
- 不破坏/删除 Plan A 现有容器与数据。
- 不使用开发默认 secret。
- 不直接抢占 443；Plan B 先用备用端口。
- 无用户明确授权不 push/不部署生产。

## 6. 恢复顺序

1. 读本文件。
2. 跑第 1 节命令核对 Git。
3. 读 `DECISION_RISK_LEDGER.md`、`FINAL_VALIDATION_SUMMARY.md`、`PRODUCTION_RELEASE_PREFLIGHT.md`、`PRODUCTION_ENVIRONMENTS.md`。
4. 按第 4 节继续，每步先出 stage brief/决策点。
