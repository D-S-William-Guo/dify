# Phase H secret-scan — synthetic pattern (S-8) result

Pattern file: `/tmp/replay-116-phase-h/secrets.pattern` (mode 0600, removed after use).
Patterns were SYNTHETIC strings (no real secrets were ever in scope for this run).
Scan ran via `check-enterprise-offline.sh -SecretsPattern <0600 file>`.

## Result

- Synthetic secret-pattern scan over images file, manifest, config archive
  members, and image-bundle layers:
  **BOOLEAN result: no synthetic pattern matched packaged content.**
- Script summary: 13 PASS / 0 FAIL / 1 NOT_RUN (exit 0). See offline-scan.log.
- The 1 NOT_RUN is the image-bundle layer content scan: Docker 29 `docker save`
  emits an OCI blob layout (`blobs/sha256/*`), which the B7 layer scan
  (`grep '*/layer.tar$'`) does not match, so layer-level content scanning
  reported NOT_RUN instead of a fake PASS. This is a known B7 gate ceiling
  (S-6 note): layer-internal secret scanning remains a Phase G runtime concern.
- Dev-default `DIFY_AGENT_SERVER_SECRET_KEY` present only in `.env.example`
  files adjacent to WARNING markers; the gate PASSed that check for the config
  package and manifest.

## Cleanup

Pattern file and all temp scan intermediates removed at teardown; no secret
values were ever printed (script prints target + hit boolean only).
