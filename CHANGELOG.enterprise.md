# 企业版变更纪要

这份文档记录企业版相对于官方 Dify 的重大改动、关键决策、踩坑结论和后续维护注意事项。

用途：

- 让新的 Codex 线程、AI IDE 或接手工程师快速建立上下文
- 记录“为什么这样改”，而不仅仅是“改了什么”
- 沉淀那些以后容易再次踩到的坑

阅读方式：

- 想了解“当前仓库应该怎么维护、怎么同步、怎么打包”，先看 [README.enterprise-maintenance.md](/D:/CodexSpace/dify/README.enterprise-maintenance.md)
- 想了解“当前性能治理为什么选路线二、以后如何沿用”，看 [README.performance-route2.md](/D:/CodexSpace/dify/README.performance-route2.md)
- 想了解“这段时间做过哪些重大改动、踩过哪些坑”，看本文件

阅读本文件时，仍以 [README.enterprise-maintenance.md](/D:/CodexSpace/dify/README.enterprise-maintenance.md) 中的维护优先级为总原则：

- 第一优先级是同步官方代码
- 第二优先级是保住企业多空间、平台管理员和智慧广场等企业功能
- 第三优先级才是路线二性能治理等阶段性优化

也就是说，本文件记录的阶段性治理成果不能反过来覆盖官方同步和企业功能维护的优先级。

---

## 2026-04-04 前端构建基线回归规则收敛

### 背景

在同步了一批官方前端更新后，企业分支的 `web` 构建没有第一时间跟上，暴露出一个很容易反复踩的维护问题：

- 真正需要长期保留的，不是某一代具体前端技术方案
- 而是“企业分支前端构建基线必须与 `upstream/main` 当前状态保持一致”这个维护原则

如果把规则写成某个具体技术名词，后续官方主线再次调整时，文档和 agent 都容易机械沿用旧结论。

### 改动内容

- 明确前端构建问题的处理顺序：
  - 先分析 `upstream/main` 当前构建方式
  - 再判断企业分支哪些内容是企业功能必需差异，哪些只是旧构建残留
  - 先恢复与官方主线一致，再叠回企业差异和路线二优化
- 明确把构建上下文卫生与源码基线分开：
  - `docker/volumes/**`、`.venv/**`、`node_modules/**`、`.codex/**` 只属于环境卫生和 Docker build context 排除项
  - 它们不应被误判为前端构建基线本身
- 把这条规则同步写入维护文档、路线二文档和 Docker skill

### 当前结论

- 以后遇到前端构建失败，先回看 `upstream/main` 当前主线，而不是先假设企业分支旧方案应该继续延长寿命
- 长期规则应保持抽象和稳定：跟随官方当前主线，而不是绑定某一代技术实现
- 这样以后无论是其他线程、其他工程师，还是新的 AI agent 接手，都能先做正确分析，再沿正确方向推进

### 后续注意

- 每次同步官方前端更新后，优先确认 `web` 的构建输入是否仍与 `upstream/main` 当前主线一致
- 只有在官方基线重新可构建之后，才继续推进路线二这类阶段性前端治理

---

## 2026-04-03 路线二性能治理第一阶段收敛

### 背景

在企业功能和通用功能持续叠加之后，性能问题已经不再只是单页热点，而是开始呈现出系统性模式：

- 后端列表接口容易被隐式查库属性放大成 `N+1`
- 前端高频 query 默认零缓存，容易在切页、切 tab、开弹窗时反复补请求
- 常驻 header、context、导航和弹层容易提前带起次级页面的重查询

分析阶段曾考虑三条路线：

- 路线一：热点驱动，快速救火
- 路线二：建立统一性能规范，中等投入，逐步治理
- 路线三：结构性重构，全面重做查询和序列化边界

最终选择路线二，因为它能保留当前项目的开发效率与架构主体，同时把高频路径逐步拉回可控范围，对企业长期分支和后续合并官方代码更友好。

### 改动内容

- 后端：
  - `Platform Admin` 工作区列表去掉 `2N+1`
  - `Enterprise Marketplace` 列表去掉多重 `N+1`
  - `apps list`、`installed-apps`、`datasets` 列表链路增加批量预取
- 前端：
  - 企业空间管理和智慧广场改为精准 invalidation
  - 平台管理员页、设置页、模型供应商页按 section / tab 挂载
  - 插件任务、插件偏好、模型供应商、知识库等高频 query 做缓存分层
  - 插件任务轮询只在 `/plugins` 页面活跃
  - 主导航中的重页面默认关闭 Next 路由预取
  - 顶部知识库导航只在知识库路由激活时拉列表
  - 去除“为了拿 refetch 而先挂 query”的隐性请求模式

### 影响模块

- 企业后台 service 与企业设置页
- `web/service/use-common.ts`
- `web/service/use-plugins.ts`
- `web/service/knowledge/use-dataset.ts`
- `web/context/provider-context-provider.tsx`
- `web/context/modal-context-provider.tsx`
- `web/app/components/header/*`

### 踩坑记录

- 知识库第二批改造时，曾因列表预取遗漏 `Tag` / `TagBinding` 导入导致创建后列表 `500`
- 模型 provider 保存报错中，存在“真实外部模型端点变化”和“本地性能回归”两类现象混在一起，需要靠日志区分
- 仅凭页面体感无法可靠定位根因，必须结合 Docker 容器日志比对请求收敛情况
- 有些问题不是页面主体造成，而是常驻 hook 或常驻组件间接触发

### 当前结论

- 路线二已被证明适合当前仓库：收益稳定、风险可控、验证成本可接受
- 当前前端最值得长期保留的做法是：
  - 缓存分层
  - 精准 invalidation
  - `enabled` 按需启用
  - 重页面导航默认不预取
  - 避免为了拿 `refetch` 而订阅 query
- 当前后端最值得长期保留的做法是：
  - 列表接口在 service 层显式批量预取
  - 避免列表序列化过程中走隐式查库属性

### 后续注意

- 后续新增功能优先沿用 [README.performance-route2.md](/D:/CodexSpace/dify/README.performance-route2.md) 中的规则，而不是继续按页面零散修补
- 以后合并官方代码时，重点复查高频冲突区，而不是机械保留全部补丁
- 这轮治理的核心价值不仅是“当前更快”，更是给企业长期分支建立了可重复应用的性能边界

---

## 2026-04-02 企业镜像命名与离线打包规则收敛

### 背景

企业版开始进入生产部署准备阶段，需要把镜像命名、打包方式、离线部署方式从“可用”收敛到“可维护、可复用、可交付”。

### 改动内容

- 明确企业正式镜像命名统一为：
  - `dify-api-enterprise:<官方版本-enterprise>`
  - `dify-web-enterprise:<官方版本-enterprise>`
- 明确 `worker` 与 `worker_beat` 运行时复用 `dify-api-enterprise`
- 新增 Linux/bash 版离线打包脚本：
  - [scripts/build-enterprise-offline.sh](/D:/CodexSpace/dify/scripts/build-enterprise-offline.sh)
- Windows 与 Linux 打包脚本统一支持三种模式：
  - `smart`
  - `rebuild`
  - `reuse`
- 默认推荐 `smart`，优先复用已验证的正式镜像

### 影响模块

- 打包脚本
- Docker 企业部署文档
- 企业维护规则

### 踩坑记录

- 早期本地曾把 `local` 镜像直接补 tag 成生产版本，这会导致镜像名看起来正确，但镜像内部 `COMMIT_SHA` 仍然是 `local`
- 如果直接用这种镜像做生产交付，版本身份不干净
- 后续判断“是否可复用”时，不能只看 tag 是否存在，还必须检查镜像内部 `COMMIT_SHA`

### 当前结论

- 生产打包不要求每次都强制重建
- 但只有在“正式 tag 存在且 `COMMIT_SHA` 与目标版本一致”时才允许直接复用
- 否则应自动重建，或在 `reuse` 模式下直接失败

### 后续注意

- 后续每次同步官方版本后，先确定企业正式版本字符串，再构建或复用镜像
- 不要继续维护 `worker` / `worker_beat` 的独立正式镜像名

---

## 2026-04-02 智慧广场复制链路修复

### 背景

智慧广场一期上线后，出现了“复制应用时前端提示失败，但工作室里其实已经生成了副本”的问题，影响用户判断，也容易诱发重复点击。

### 改动内容

- 修复智慧广场复制导入的事务边界问题
- 补齐复制中的状态反馈、失败提示与防重复点击逻辑
- 复制成功后支持直接跳转到复制出的应用/工作流编辑页面

### 影响模块

- 企业智慧广场后端复制链路
- DSL 导入服务
- 智慧广场前端详情弹窗与复制交互

### 踩坑记录

- 曾出现“实际成功但前端显示失败”
- 根因不是简单的前端 toast，而是后端事务边界不正确：
  - 企业广场复制链路里 session 生命周期处理不当
  - `app_dsl_service` 中存在在上下文事务内提前 `commit` 的问题
- 事务提前关闭后，后续逻辑抛出 `closed transaction` 异常，导致最终状态被误判为失败

### 当前结论

- 以后如果再改“复制应用 / 导入 DSL / 智慧广场使用”相关逻辑，优先回看这里
- 这条链路最关键的是：
  - session 生命周期
  - 事务边界
  - 前端状态与后端真实结果的一致性

### 后续注意

- 任何复制导入相关改动，都应至少验证：
  - 成功时是否只生成一个副本
  - 失败提示是否和真实结果一致
  - 用户连续点击时是否会生成重复副本

---

## 2026-04-01 智慧广场一期落地

### 背景

希望在企业空间隔离的前提下，让高频应用能力可以在企业内部被提交、审核、展示和复制，从而形成企业内部复用的“智慧广场”。

### 改动内容

- 新增企业内部“智慧广场”一期能力
- 支持空间侧提交应用到智慧广场
- 支持平台管理员审批、拒绝、下架
- 支持其他空间浏览通过审核的应用，并复制到当前空间
- 设计上坚持“复制导入”，而不是跨空间直接共用运行时

### 影响模块

- 企业后台接口与模型
- 应用提交与审核流程
- 平台管理员页
- Explore/智慧广场页面

### 踩坑记录

- 提交到智慧广场时曾出现数据库字段类型映射错误，根因是 ORM 映射与实际表字段类型不一致
- 普通空间所有者最初缺少明显入口，导致页面可访问但不易发现
- 复制链路最初提示不清晰，用户容易重复点击

### 当前结论

- 一期能力以“应用级资产”为主，不做跨空间实时共享运行时
- 工具能力通过“应用封装 + 复制导入”体现，而不是单独做成独立商品

### 后续注意

- 如果未来扩展二期，优先考虑：
  - 更多审核信息
  - 提交人/来源展示
  - 复制依赖检查
  - 更完善的企业广场运营管理

---

## 2026-04-01 企业空间管理功能稳定化

### 背景

企业空间管理能力是企业版最早的核心定制功能之一，需要确保平台管理员视角、空间隔离和工作区管理链路稳定可用。

### 改动内容

- 企业空间管理页面与平台管理员能力已落地
- 支持企业空间创建、成员管理、角色操作等核心能力
- 当前企业分支默认把这块能力视为长期维护范围

### 影响模块

- 平台管理员识别
- 企业空间管理页
- 工作区创建与成员管理链路

### 踩坑记录

- 平台管理员页的部分弹窗曾被外层设置层遮挡，表现为点击无反应
- 这类问题不一定是后端没收到请求，也可能是前端层级与弹窗挂载位置错误

### 当前结论

- 企业空间管理已经是当前企业版的基础能力，不再是实验性功能
- 以后同步官方更新时，这部分应始终纳入最小回归验证

### 后续注意

- 每次合并 upstream 后，优先回归：
  - 平台管理员识别
  - 企业空间创建
  - 成员邀请与角色调整
  - 企业设置页弹窗交互
- 版本规则收敛：
  - 同步官方后，企业镜像版本必须从 `api/pyproject.toml` 与 `web/package.json` 读取官方版本
  - 只有两者一致时，才允许生成 `官方版本-enterprise`
  - 本地验证、compose 重建、离线打包和生产部署必须沿用同一个 `DIFY_ENTERPRISE_VERSION`
